// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // output: "export",
  images: {
    domains: ["deltainvested.com"],
  },
};

module.exports = nextConfig;
